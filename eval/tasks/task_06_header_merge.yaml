task_id: task_06_header_merge
description: >
  Make the client header merge behavior explicit and add a regression test
  proving per-request headers take precedence over instance defaults when
  the same header key appears in both.
difficulty: medium
expected_files:
  - httpx/_client.py
  - tests/test_grabonai_header_merge.py
expected_behavior: "per-request headers override instance headers for same key"
should_be_impossible: false
