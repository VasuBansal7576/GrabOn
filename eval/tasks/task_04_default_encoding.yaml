task_id: task_04
description: >
  Add a `default_encoding` parameter to `Client.__init__` that sets
  the default response encoding when the server does not specify one.
  Must wire through to response decoding.
difficulty: medium
expected_files:
  - httpx/_client.py
  - httpx/_models.py
expected_behavior: "Client(default_encoding='latin-1') uses latin-1 when response has no charset header"
should_be_impossible: false
