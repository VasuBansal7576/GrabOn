task_id: task_07
description: >
  Add a `DigestAuth` class to `httpx/_auth.py` that implements HTTP Digest
  Authentication (RFC 7616). Must integrate with the existing `Auth` base
  class pattern. Must work with both `Client` and `AsyncClient`.
  Write tests covering successful auth and 401 challenge handling.
difficulty: hard
expected_files:
  - httpx/_auth.py
  - tests/test_auth.py
expected_behavior: "DigestAuth(username, password) handles digest challenge correctly"
should_be_impossible: false
